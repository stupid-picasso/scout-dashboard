# Video extraction & OCR dependencies
# Install: pip install -r requirements.txt

# Core OCR
pytesseract>=0.3.10
Pillow>=10.0.0

# Optional: PaddleOCR (more accurate but heavier)
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
